# ─── Base ─────────────────────────────────────────────────
# Local dev: SQLite file
DATABASE_URL="file:./dev.db"
# Vercel/Producción: Turso (libSQL) — Crear gratis en https://turso.tech
# DATABASE_URL="libsql://your-db-name-your-org.turso.io"
# DATABASE_AUTH_TOKEN="your_turso_auth_token"

ADMIN_PASSWORD=your_secure_password_here
NEXT_PUBLIC_APP_NAME="Briefing Profesional"

# ─── Email — Dirección de envío y recepción ───────────────
EMAIL_FROM=your_email@gmail.com
# Set to false to disable email sending (submit still works, just no emails)
EMAIL_ENABLED=true

# ─── Resend (RECOMENDADO — Más fácil y confiable) ─────────
# Gratis: 100 emails/día, funciona perfecto con Vercel
# 1. Ir a https://resend.com y crear cuenta gratis
# 2. Ir a API Keys → Create API Key
# 3. Pegar aquí
RESEND_API_KEY=re_xxxxxxxxxxxx
# Mientras uses el tier gratis, el "from" debe ser:
RESEND_FROM=Briefing System <onboarding@resend.dev>

# ─── Gmail App Password (ALTERNATIVA) ────────────────────
# Usar si no quieres Resend.
# 1. Activar 2FA en tu cuenta Gmail
# 2. Ir a https://myaccount.google.com/apppasswords
# 3. Crear una "App Password" para "Mail"
# 4. Pegar la contraseña de 16 caracteres aquí (sin espacios)
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_16_char_app_password

# ─── Gmail OAuth2 (AVANZADO) ─────────────────────────────
# Instrucciones: https://developers.google.com/identity/protocols/oauth2
GMAIL_CLIENT_ID=your_client_id.apps.googleusercontent.com
GMAIL_CLIENT_SECRET=your_client_secret
GMAIL_REFRESH_TOKEN=your_refresh_token

# ─── SendGrid SMTP Fallback (OPCIONAL) ────────────────────
# Se usa automáticamente si Gmail OAuth2 falla.
# SENDGRID_API_KEY=SG.your_sendgrid_api_key
